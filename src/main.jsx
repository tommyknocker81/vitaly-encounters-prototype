import React from "react";
import { createRoot } from "react-dom/client";
import EncountersPrototype from "./EncountersPrototype.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EncountersPrototype />
  </React.StrictMode>
);
